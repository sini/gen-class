# gen-class — lib/apply.nix (STUB, filled by Tasks 4 + 7).
# Core-application mechanisms: per-class oracle (`mkCore`), merge/extend application, the
# invariance probe (tier 1), and `applyCoreFixed` via the injected `merge` kernel (tier 2), spec §2.3.
{
  prelude,
  contract,
  merge,
}:
{ }

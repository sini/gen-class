# gen-class — lib/gate.nix (STUB, filled by Task 5).
# The hard-fail byte gate (`gateCore`) + the pure halves of the STOP-on-diff counter comparators,
# spec §2.4. The public API exposes NO gate-free reuse path.
{ prelude }:
{ }
